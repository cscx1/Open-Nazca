# 🔒 Open Nazca Security Scan Report

## Scan Information

- **File:** 83b10bfc-ba2d-47e8-b842-eeb7d11ed961.py
- **Language:** python
- **Scan Time:** 2026-03-22 12:16:21
- **Scan ID:** local-1774196178

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 38 |
| 🟡 Medium | 0 |
| 🔵 Low | 0 |
| **Total** | **38** |

### Vulnerabilities by Type

- **Unsafe Reflection:** 25
- **Attribute Injection:** 2
- **Mass Assignment:** 2
- **ReDoS:** 3
- **Code Execution:** 3
- **Path Traversal:** 3

---

## Detailed Findings

### 🟠 HIGH Severity

#### 1. Unsafe Reflection

**Location:** Line 536  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
533   |             return obj if not callable(obj) else None
534   | 
535   |         return [
536 → |             {key: noncallable(getattr(field, key)) for key in keys}
537   |             for field in self
538   |             if field.readable and not field.type == "password"
539   |         ]
540   | 
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 2. Unsafe Reflection

**Location:** Line 615  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
612   |     def _validate(self, **vars):
613   |         errors = Row()
614   |         for key, value in iteritems(vars):
615 → |             value, error = getattr(self, key).validate(value, vars.get("id"))
616   |             if error:
617   |                 errors[key] = error
618   |         return errors
619   | 
```

**Verdict:** Out-of-scope — Input validation or sanitization present on same line.

**Evidence:** Out-of-scope

---

#### 3. Unsafe Reflection

**Location:** Line 709  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
706   |                 # include fields if
707   |                 if (
708   |                     k in self.fields
709 → |                     and (allow_id or getattr(self, k).type != "id")  # are valid
710   |                     and (not writable_only or getattr(self, k).writable)  # are not ids
711   |                 )  # are writable
712   |             ]
713   |         )
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 4. Unsafe Reflection

**Location:** Line 710  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
707   |                 if (
708   |                     k in self.fields
709   |                     and (allow_id or getattr(self, k).type != "id")  # are valid
710 → |                     and (not writable_only or getattr(self, k).writable)  # are not ids
711   |                 )  # are writable
712   |             ]
713   |         )
714   | 
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 5. Unsafe Reflection

**Location:** Line 721  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
718   |         for k, v in iteritems(key):
719   |             if k in self._primarykey:
720   |                 if query:
721 → |                     query = query & (getattr(self, k) == v)
722   |                 else:
723   |                     query = getattr(self, k) == v
724   |             else:
725   |                 raise SyntaxError(
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 6. Unsafe Reflection

**Location:** Line 723  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
720   |                 if query:
721   |                     query = query & (getattr(self, k) == v)
722   |                 else:
723 → |                     query = getattr(self, k) == v
724   |             else:
725   |                 raise SyntaxError(
726   |                     "Field %s is not part of the primary key of %s"
727   |                     % (k, self._tablename)
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 7. Unsafe Reflection

**Location:** Line 747  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
744   |             )
745   |         elif key is not None:
746   |             try:
747 → |                 return getattr(self, key)
748   |             except:
749   |                 raise KeyError(key)
750   | 
751   |     def __call__(self, key=DEFAULT, **kwargs):
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 8. Unsafe Reflection

**Location:** Line 793  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
790   |         elif kwargs:
791   |             query = reduce(
792   |                 lambda a, b: a & b,
793 → |                 [getattr(self, k) == v for k, v in iteritems(kwargs)],
794   |             )
795   |             return (
796   |                 self._db(query)
797   |                 .select(
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 9. Unsafe Reflection

**Location:** Line 856  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
853   | 
854   |     def __iter__(self):
855   |         for fieldname in self.fields:
856 → |             yield getattr(self, fieldname)
857   | 
858   |     def __repr__(self):
859   |         return "<Table %s (%s)>" % (self._tablename, ", ".join(self.fields()))
860   | 
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 10. Unsafe Reflection

**Location:** Line 905  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
902   |         table_fieldnames = set(self.fields)
903   |         empty_fieldnames = OrderedDict((name, name) for name in self.fields)
904   |         for name in list(input_fieldnames & table_fieldnames):
905 → |             field = getattr(self, name)
906   |             value = fields[name]
907   |             if field.filter_in and not isinstance(value, Expression):
908   |                 if callable(value):
909   |                     value = value()
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 11. Unsafe Reflection

**Location:** Line 944  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
941   |         empty_fieldnames, new_fields = self._filter_fields_for_operation(fields)
942   |         to_compute = []
943   |         for name in empty_fieldnames:
944 → |             field = getattr(self, name)
945   |             if field.compute:
946   |                 to_compute.append((name, field))
947   |             elif field.default is not None:
948   |                 new_fields[name] = (field, field.default)
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 12. Unsafe Reflection

**Location:** Line 957  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
954   |         empty_fieldnames, new_fields = self._filter_fields_for_operation(fields)
955   |         to_compute = []
956   |         for name in empty_fieldnames:
957 → |             field = getattr(self, name)
958   |             if field.compute:
959   |                 to_compute.append((name, field))
960   |             if field.update is not None:
961   |                 new_fields[name] = (field, field.update)
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 13. Unsafe Reflection

**Location:** Line 1063  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1060   |                 query = None
1061   |                 for key, value in iteritems(_key):
1062   |                     if query is None:
1063 → |                         query = getattr(self, key) == value
1064   |                     else:
1065   |                         query = query & (getattr(self, key) == value)
1066   |                 myset = self._db(query)
1067   |             updated = myset.update(**new_fields)
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 14. Unsafe Reflection

**Location:** Line 1065  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1062   |                     if query is None:
1063   |                         query = getattr(self, key) == value
1064   |                     else:
1065 → |                         query = query & (getattr(self, key) == value)
1066   |                 myset = self._db(query)
1067   |             updated = myset.update(**new_fields)
1068   |         return {"id": record and record.id, "updated": updated, "errors": errors}
1069   | 
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 15. Unsafe Reflection

**Location:** Line 1096  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1093   |             else:
1094   |                 required_keys = {}
1095   |                 for key, value in iteritems(fields):
1096 → |                     if getattr(self, key).required:
1097   |                         required_keys[key] = value
1098   |                 record = self(**required_keys)
1099   |                 _key = required_keys
1100   |         elif isinstance(_key, dict):
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 16. Unsafe Reflection

**Location:** Line 1110  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1107   |             if hasattr(self, "_primarykey"):
1108   |                 primary_keys = {}
1109   |                 for key in self._primarykey:
1110 → |                     primary_keys[key] = getattr(record, key)
1111   |                 response["id"] = primary_keys
1112   |         else:
1113   |             response = self.validate_and_insert(**fields)
1114   |         return response
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 17. Unsafe Reflection

**Location:** Line 1231  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1228   | 
1229   |         def is_id(colname):
1230   |             if colname in self:
1231 → |                 return getattr(self, colname).type == "id"
1232   |             else:
1233   |                 return False
1234   | 
1235   |         first = True
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 18. Unsafe Reflection

**Location:** Line 1249  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1246   |                     if is_id(colname):
1247   |                         cid = colname
1248   |                     elif colname in self.fields:
1249 → |                         cols[colname] = getattr(self, colname)
1250   |                     if colname == unique:
1251   |                         unique_idx = i
1252   |             elif len(line) == len(colnames):
1253   |                 # every other line contains instead data
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 19. Unsafe Reflection

**Location:** Line 1283  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1280   |                         )
1281   |                     # create new id until we get the same as old_id+offset
1282   |                     while curr_id < csv_id + id_offset[self._tablename]:
1283 → |                         self._db(getattr(self, cid) == curr_id).delete()
1284   |                         curr_id = inserting(**ditems)
1285   |                 # Validation. Check for duplicate of 'unique' &,
1286   |                 # if present, update instead of insert.
1287   |                 elif not unique_idx:
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 20. Unsafe Reflection

**Location:** Line 1291  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1288   |                     new_id = inserting(**ditems)
1289   |                 else:
1290   |                     unique_value = line[unique_idx]
1291 → |                     query = getattr(self, unique) == unique_value
1292   |                     record = self._db(query).select().first()
1293   |                     if record:
1294   |                         record.update_record(**ditems)
1295   |                         new_id = record[self._id.name]
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 21. Unsafe Reflection

**Location:** Line 1332  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1329   |         other["ALL"] = SQLALL(other)
1330   |         other["_tablename"] = alias
1331   |         for fieldname in other.fields:
1332 → |             tmp = getattr(self, fieldname).clone()
1333   |             tmp.bind(other)
1334   |             other[fieldname] = tmp
1335   |         if "id" in self and "id" not in other.fields:
1336   |             other["id"] = other[self.id.name]
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 22. Unsafe Reflection

**Location:** Line 1401  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
1398   | 
1399   |     def __getitem__(self, key):
1400   |         try:
1401 → |             return getattr(self, key)
1402   |         except AttributeError:
1403   |             raise KeyError(key)
1404   | 
1405   |     def __setitem__(self, key, value):
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 23. Attribute Injection

**Location:** Line 1560  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
setattr() called with a non-literal attribute name. An attacker controlling the attribute name can overwrite critical object attributes (is_admin, __class__, etc.) leading to privilege escalation or code execution.

**Code Snippet:**
```
1557   | 
1558   |     def __new__(cls, *args, **kwargs):
1559   |         for name, wrapper in iteritems(cls._dialect_expressions_):
1560 → |             setattr(cls, name, _expression_wrap(wrapper))
1561   |         new_cls = super(Expression, cls).__new__(cls)
1562   |         return new_cls
1563   | 
1564   |     def __init__(
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 24. Attribute Injection

**Location:** Line 2222  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
setattr() called with a non-literal attribute name. An attacker controlling the attribute name can overwrite critical object attributes (is_admin, __class__, etc.) leading to privilege escalation or code execution.

**Code Snippet:**
```
2219   |             stype = self.type.type
2220   |         self._itype = REGEX_TYPE.match(stype).group(0) if stype else None
2221   |         for key in others:
2222 → |             setattr(self, key, others[key])
2223   | 
2224   |     @staticmethod
2225   |     def _todate(value, regex=re.compile(r"^\d\d\d\d-\d\d-\d\d$")):
2226   |         if value is None:
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 25. Unsafe Reflection

**Location:** Line 2528  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
2525   |         if not (sanitize and not (self.readable or self.writable)):
2526   |             for attr in attrs:
2527   |                 if flat:
2528 → |                     d.update({attr: flatten(getattr(self, attr))})
2529   |                 else:
2530   |                     d.update({attr: getattr(self, attr)})
2531   |             d["fieldname"] = d.pop("name")
2532   |         return d
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 26. Unsafe Reflection

**Location:** Line 2530  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
2527   |                 if flat:
2528   |                     d.update({attr: flatten(getattr(self, attr))})
2529   |                 else:
2530 → |                     d.update({attr: getattr(self, attr)})
2531   |             d["fieldname"] = d.pop("name")
2532   |         return d
2533   | 
2534   |     def referenced_field(self):
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 27. Unsafe Reflection

**Location:** Line 3506  
**Detector:** UnsafeReflectionDetector  
**Confidence:** 90%

**Description:**  
getattr() called with a non-literal attribute name. If the attribute name comes from user input, an attacker can access arbitrary attributes including private methods and dunder methods.

**Code Snippet:**
```
3503   |                 updated = False
3504   |                 for attribute in attributes:
3505   |                     if attribute[0] != "_":
3506 → |                         method = getattr(virtualfields, attribute)
3507   |                         if hasattr(method, "__lazy__"):
3508   |                             box[attribute] = VirtualCommand(method, row)
3509   |                         elif type(method) == types.MethodType:
3510   |                             if not updated:
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 28. Mass Assignment

**Location:** Line 2289  
**Detector:** TypeConfusionDetector  
**Confidence:** 88%

**Description:**  
__dict__.update() called with external data. This overwrites all object attributes at once, allowing attackers to inject arbitrary attributes.

**Code Snippet:**
```
2285   |     def clone(self, point_self_references_to=False, **args):
2286   |         field = copy.copy(self)
2287   |         if point_self_references_to and self.type == "reference %s" % self._tablename:
2288   |             field.type = "reference %s" % point_self_references_to
2289 → |         field.__dict__.update(args)
2290   |         field.db = field._db = None
2291   |         field.table = field._table = None
2292   |         field.tablename = field._tablename = None
2293   |         if self._db and self._rname == self._db._adapter.sqlsafe_field(self.name):
2294   |             # Reset the name because it may need to be requoted by bind()
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 29. Mass Assignment

**Location:** Line 3511  
**Detector:** TypeConfusionDetector  
**Confidence:** 88%

**Description:**  
__dict__.update() called with external data. This overwrites all object attributes at once, allowing attackers to inject arbitrary attributes.

**Code Snippet:**
```
3507   |                         if hasattr(method, "__lazy__"):
3508   |                             box[attribute] = VirtualCommand(method, row)
3509   |                         elif type(method) == types.MethodType:
3510   |                             if not updated:
3511 → |                                 virtualfields.__dict__.update(row)
3512   |                                 updated = True
3513   |                             box[attribute] = method()
3514   |         return self
3515   | 
3516   |     def __add__(self, other):
```

**Verdict:** Unverified — Static match only; no verified data flow.

**Evidence:** Unverified

---

#### 30. ReDoS

**Location:** Line 275  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches re.match()

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from field (line 263) to re.match (line 275) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `re.match`

**Evidence:** Confirmed | Confirmed Reachable | path: field → re.match

**Attack Path:** `field` (line 263) -> `re.match` (line 275)

---

#### 31. Code Execution

**Location:** Line 1492  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches self._compile() via keyword 'cte_collector'

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from cte_collector (line 1483) to self._compile (line 1492) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `self._compile`

**Evidence:** Confirmed | Confirmed Reachable | path: cte_collector → self._compile

**Attack Path:** `cte_collector` (line 1483) -> `self._compile` (line 1492)

---

#### 32. Code Execution

**Location:** Line 1506  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches r._compile() via keyword 'cte_collector'

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from cte_collector (line 1483) to r._compile (line 1506) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `r._compile`

**Evidence:** Confirmed | Confirmed Reachable | path: cte_collector → r._compile

**Attack Path:** `cte_collector` (line 1483) -> `r._compile` (line 1506)

---

#### 33. Code Execution

**Location:** Line 1521  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches self._compile()

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from outer_scoped (line 1516) to self._compile (line 1521) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `self._compile`

**Evidence:** Confirmed | Confirmed Reachable | path: outer_scoped → self._compile

**Attack Path:** `outer_scoped` (line 1516) -> `self._compile` (line 1521)

---

#### 34. ReDoS

**Location:** Line 2309  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches re.search()

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from file (line 2298) to re.search (line 2309) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `re.search`

**Evidence:** Confirmed | Confirmed Reachable | path: file → re.search

**Attack Path:** `file` (line 2298) -> `file` (line 2305) -> `filename` (line 2307) -> `re.search` (line 2309)

---

#### 35. Path Traversal

**Location:** Line 2353  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches open()

**Code Snippet:**
```

```

**Reachability:** [ok] Reachability Eliminated  
Sanitiser(s) detected between source (line 2298) and sink (line 2353): os.path.basename. Reachability is considered eliminated, though manual confirmation of sanitiser effectiveness is recommended.

**Verdict:** Unverified — No rule matched; default unverified.

**Sink API:** `open`

**Evidence:** Unverified | Reachability Eliminated | path: path → open

**Attack Path:** `path` (line 2298) -> `path` (line 2347) -> `pathfilename` (line 2352) -> `open` (line 2353)

---

#### 36. Path Traversal

**Location:** Line 2389  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches self.uploadfs.open()

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from name (line 2364) to self.uploadfs.open (line 2389) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `self.uploadfs.open`

**Evidence:** Confirmed | Confirmed Reachable | path: name → self.uploadfs.open

**Attack Path:** `name` (line 2364) -> `self.uploadfs.open` (line 2389)

---

#### 37. Path Traversal

**Location:** Line 2397  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches open()

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from name (line 2364) to open (line 2397) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `open`

**Evidence:** Confirmed | Confirmed Reachable | path: name → open

**Attack Path:** `name` (line 2364) -> `fullname` (line 2394) -> `open` (line 2397)

---

#### 38. ReDoS

**Location:** Line 2401  
**Detector:** StaticAnalysisPipeline  
**Confidence:** 90%

**Description:**  
Tainted data reaches re.match()

**Code Snippet:**
```

```

**Reachability:** [X] Confirmed Reachable  
Tainted data flows from name (line 2400) to re.match (line 2401) with no sanitiser detected. The path is considered reachable.

**Verdict:** Confirmed — Verified attack path from source to sink.

**Sink API:** `re.match`

**Evidence:** Confirmed | Confirmed Reachable | path: name → re.match

**Attack Path:** `name` (line 2400) -> `re.match` (line 2401)

---

